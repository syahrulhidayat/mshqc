#include <mshqc/ci/sparse_coo.h>

namespace mshqc {
namespace ci {

// All methods are defined inline in the header for now to keep linkage simple.
// If heavy implementations are added later, move them here.

} // namespace ci
} // namespace mshqc
